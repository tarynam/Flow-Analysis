---
title: "flowWorkspace Code"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(eval = TRUE, error=TRUE, warning=TRUE)
```

```{r, eval=TRUE}
library(flowWorkspace)
```

```{r, eval=TRUE}
datadir="/Users/oldmac/Desktop/Day Lab/FlowJo/"
wsfile <- list.files(datadir, pattern=".xml",full=TRUE)
ws <- openWorkspace(wsfile)
summary(ws)
str(ws)
```

```{r}
G<-parseWorkspace(ws, name="HD")
```

```{r}
G<-parseWorkspace(ws, name="HD", path=datadir)
```

